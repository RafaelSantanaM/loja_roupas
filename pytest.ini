[pytest]
; "pythonpath = ." adiciona a raiz do projeto ao caminho de importação
; do Python durante os testes -- sem isso, "from api import app" dentro
; de tests/conftest.py falharia, porque api.py mora um nível acima.
pythonpath = .
testpaths = tests
